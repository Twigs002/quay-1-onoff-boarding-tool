# Runbook - Portal maps (browser systems)

Where each screen lives for the worker-executed portals. Keep this current when a portal
changes UI - a stale map here is the usual root cause of a `dialfire`/`property24` incident.

## property24 ([[systems/property24]])
- admin URL:
- add agent: (path / screenshot)
- deactivate agent: (path / screenshot)

## cma ([[systems/cma]]) - OTP gated
- login + OTP notes:
- create user:
- disable user:

## dialfire ([[systems/dialfire]]) - NEEDS-PORTAL-MAP
- user-management URL (unconfirmed):
- add seat:
- remove seat:
