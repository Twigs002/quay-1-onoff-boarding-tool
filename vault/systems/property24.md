---
system: property24
executes: worker
api: no
---

# property24

- home: the Python + Playwright worker (no usable API).
- create: browser admin -> add agent (also auto-links via the Quay1 Google login).
- deactivate: browser -> deactivate agent.
- worker claims the row by CAS on the Provisioning Queue (CONTRACTS s3).
