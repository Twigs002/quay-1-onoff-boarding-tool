---
system: dialfire
executes: worker
api: no
status: needs-portal-map   # portal path unconfirmed
---

# dialfire

- home: worker. create/remove agent seat via browser.
- portal path for user management is **unconfirmed** (NEEDS-PORTAL-MAP) - provisioner is
  scaffolded, verify before flipping DRY_RUN=0.
- only provisioned when the Quay1 `dialfire` program is ticked.
